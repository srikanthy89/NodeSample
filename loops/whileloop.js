const arr = [1,2,3,4,5,-21,21,34,14];
let i = 0;

while(arr[i] > 0) {
    // do some operation here
    //console.log(`Yes ${arr[i]} is greater than 0`);
    i++
}

for (let i =0; i < arr[i] > 0; i++) {
    console.log(`Yes ${arr[i]} is greater than 0`);
}

// Increament happens only if the condition is satisfied